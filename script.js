/* import $ from "https://cdn.skypack.dev/jquery@3.6.1";

class Clicked {
	constructor() {

		$( '.one__button' ).on( 'click', ( e ) => {
			this.clickIt( e );
		});
	}
  
  clickIt( e )  {
    const one = $( e.target.closest( '.one' ) );

    
    one.toggleClass( 'one--clicked' );
  }
}

new Clicked();

*/
